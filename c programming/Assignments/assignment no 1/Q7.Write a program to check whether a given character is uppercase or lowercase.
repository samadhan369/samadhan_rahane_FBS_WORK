#include<stdio.h>
int main()
{
	char ch;
	printf("enter the character : ");
	scanf("%d",ch);
	if(ch>='A' && ch<='Z')
	{
	
	printf("character is upper case");
}

else
{
	printf("character is lower case");
}
}
