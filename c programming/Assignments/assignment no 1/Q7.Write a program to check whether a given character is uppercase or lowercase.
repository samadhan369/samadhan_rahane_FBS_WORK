#include<stdio.h>
int main()
{
	char ch;
	printf("enter the character : ");
	scanf("%c",&ch);
	if(ch>='A' && ch<='Z')
	printf("character %c is upper case",ch);
else if(ch>='a' && ch<='z')
	printf("character %c is lower case",ch);
else
   printf("invalid character ");
   return 0;
}
