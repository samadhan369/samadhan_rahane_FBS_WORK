#include <stdio.h>

int main() {
    int number, i;

    printf("Enter the start number: ");
    scanf("%d", &number);
    printf("Enter the stop number: ");
    scanf("%d", &i);
    while (number <= i) {
        printf("number = %d\n", number);
        number++;
    }
    return 0;
}
