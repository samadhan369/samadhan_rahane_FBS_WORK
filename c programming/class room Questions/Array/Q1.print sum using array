#include <stdio.h>
int main()
{
    int arr[5];
    int i, sum = 0;
    printf("Enter 5 numbers:\n");
    for(i = 0; i <= 4; i++) {
        scanf("%d", &arr[i]);  
        sum = sum + arr[i];    
    }
    printf("Sum of numbers = %d\n", sum);
}
