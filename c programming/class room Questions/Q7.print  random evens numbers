#include<stdio.h>
int main()
{
	int number,n,n1=0;
	printf("enter the number:");
	scanf("%d",&number);
	printf("stop the number:");
	scanf("%d",&n);

	while(n1<n)
	{
		printf("\nnumber=%d",number);
		number=number+2;
		n1++;
	}
	
	return 0;
}
