#include <stdio.h>
int voting();//dec
int main()
{
voting(); //fc
}
    int voting() //def
    {
    int age;
    printf("Enter the person's age: ");
    scanf("%d", &age);
    if (age >= 18)
        printf("Person is eligible to vote.\n");
    else
        printf("Person is not eligible to vote.\n");
    return 0;
}
