#include<stdio.h>

void main()
{
    int p = 5, q = 7 + 1;  
    int r = p++;          
    while (r <= 10)
    {
        r = q++ + ++p;    
        r++;             
    }
    printf("p=%d\n q=%d\n r=%d\n", p, q,r);
}
